[package]
name = "snapshort-usecases"
version.workspace = true
edition.workspace = true

[dependencies]
web-time.workspace = true
miniter-domain.workspace = true
miniter-usecases.workspace = true
snapshort-infra-media = { path = "../infra-media" }
snapshort-infra-render = { path = "../infra-render" }

flume.workspace = true
futures.workspace = true

serde.workspace = true
serde_json.workspace = true

thiserror.workspace = true
tracing.workspace = true
uuid.workspace = true
chrono.workspace = true
async-trait = { workspace = true }

# Native-only backends (tokio runtimes, sqlite, media engines). The wasm
# shell builds only the pure command/event/type surface + services-free bus.
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
snapshort-infra-db = { path = "../infra-db" }
tokio.workspace = true
tokio-util.workspace = true

[dev-dependencies]
tokio-test.workspace = true
tempfile.workspace = true
